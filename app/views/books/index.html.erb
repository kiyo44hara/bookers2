<% @books.each do |book| %>
<div>
  <%= image_tag 'sample-author1.jpg' %>
  
  <%= link_to book_path(book.id) do %>
    <%= book.title %>
  <% end %>
  <p>Opinion <%= book.body %></p>
</div>
<% end %>

<!--詳細画面リンクを＝Titleカラムそのままにしたい-->